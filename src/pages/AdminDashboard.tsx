// Ganti seluruh isi file AdminDashboard.tsx dengan ini untuk sementara

import React from 'react';
import Layout from '@/components/Layout'; // Kita tetap pakai layout

const AdminDashboard = () => {
  return (
    <Layout>
      <div style={{ padding: '40px' }}>
        <h1 style={{ fontSize: '2rem' }}>Tes Tampilan Admin Dashboard</h1>
        <p>Jika halaman ini muncul, berarti masalah ada pada kode asli AdminDashboard.</p>
      </div>
    </Layout>
  );
};

export default AdminDashboard;
